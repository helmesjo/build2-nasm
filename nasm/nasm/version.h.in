// # This defines the following macros:
// #
// # version.h:
// # NASM_MAJOR_VER
// # NASM_MINOR_VER
// # NASM_SUBMINOR_VER       -- this is zero if no subminor
// # NASM_PATCHLEVEL_VER     -- this is zero if no patchlevel
// # NASM_SNAPSHOT           -- if snapshot
// # NASM_VERSION_ID         -- version number encoded
// # NASM_VER                -- whole version number as a string

#define NASM_MAJOR_VER       $nasm.version.major$
#define NASM_MINOR_VER       $nasm.version.minor$
#define NASM_SUBMINOR_VER    $nasm.version.patch$
#define NASM_PATCHLEVEL_VER  0
#define NASM_SNAPSHOT        $nasm.version.snapshot_sn$ULL
#define NASM_VERSION_ID      "$nasm.version.snapshot_id$"
#define NASM_VER             "$nasm.version.project$"
